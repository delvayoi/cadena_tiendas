 {% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {#{{ form_widget(form) }}
    <button class="btn btn-sm btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}



#}
      <section class="content">
      <div class="container-fluid">
        <div class="row">
          <!-- left column -->
          <div class="col-md-6">
            <!-- general form elements -->
            <div class="card card-dark">
              <div class="card-header">
                <h3 class="card-title">Trailer </h3>
              </div>
              <!-- /.card-header -->
              <!-- form start -->            
             
             
                <div class="card-body">
               
                  <div class="form-group">
                    <label for="exampleInputEmail1">Código establecimiento</label>
                    {{form_widget(form.estabtrailer)}}
                   
                  </div>
                  
                   <div class="form-group">
                    <label for="exampleInputPassword1">Nevera</label>
                       {{form_widget(form.nevera)}}
                    
                 <div class="form-group">
                    <label for="exampleInputPassword1">Contrato helado</label>
                    {{form_widget(form.contratohel)}}
                   
                  </div> 
                    <div class="form-group">
                    <label for="exampleInputPassword1">Fecha</label>
                    {{form_widget(form.fecha)}}                       
                  
                </div>
                <!-- /.card-body -->              
             
            </div>
            </div>
            </div>
            </div>
            </section>
    <button class="btn btn btn-outline-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
