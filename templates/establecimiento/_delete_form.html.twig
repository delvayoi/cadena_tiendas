<form method="post" action="{{ path('app_establecimiento_delete', {'id': establecimiento.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ establecimiento.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
